{
  "name": "@research.actor/cli",
  "version": "0.4.5",
  "description": "CLI for research — cached codebase analysis for AI agents",
  "homepage": "https://research.actor",
  "repository": {
    "type": "git",
    "url": "https://github.com/mateffy/research.actor.git",
    "directory": "packages/cli"
  },
  "type": "module",
  "bin": {
    "research": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --format esm && bun run typecheck",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@research.actor/core": "workspace:*",
    "citty": "^0.1.6"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.0.0"
  }
}
